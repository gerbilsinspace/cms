/* global jest */
jest.mock('firebase')
jest.mock('vue-router')
jest.mock('@/router')
import { actions } from '@/store/actions'

describe('vuex actions', () => {

  it('should have a userSignUp ation that should sign up if the user has valid credentials', () => {
  })
})
